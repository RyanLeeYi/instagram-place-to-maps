# agy 可靠度量測（F26 重試機制上線後）

每支影片跑 3 次 `GeminiVideoExtractor.extract()`（內建重試，`agy_max_attempts=3`、`agy_total_timeout=0`），記錄整次呼叫（內部可能重試了好幾輪）最後有沒有成功的比率。

- (a) DT2w2PVgXo3 北投市場: 3/3（100%）
- (b) DUzYwaAElyG 酒場清志郎: 2/3（67%）

合計 5/6（83%）。對照基準：F26 上線前的單次呼叫實測約 16 次 8 失敗（約 50%）。

## 量測時順手發現的兩件事

**agy 對同一支影片會讀出不同的字。** 案例 (b) 第 1 次回「酒場 **清治郎**」，第 3 次回「酒場 **清志郎**」。
`scripts/f22_regression.py` 比對的是「清志郎」子字串，所以那條案例會隨 agy 的心情變紅——
不是程式壞了，是模型不穩。目前回歸腳本走的是快取材料（不呼叫 agy），所以不受影響；
但哪天要把它改成真的呼叫 agy，這個脆弱性就會浮出來。

**三連敗仍然存在。** 案例 (b) 有一次三次嘗試全部「回應裡找不到 JSON 物件」。
重試把「一半機率沒功能」壓成「少數影片沒功能」，不是把它消滅。
2026-08-25 另有一支 Threads 影片連續四次全敗，同一種模式。

## 量測條件

- 日期：2026-08-25
- `agy` 版本 1.1.20
- 單次成功耗時 25-60 秒；失敗（重試耗盡）約 47 秒
